## Задача 1. Хранение плейлистов (Room)
- [x] Заменить использование моков на БД в методах `PlaylistsRepositoryImpl`,
- [x] Заменить использование моков на БД в методах `TracksRepositoryImpl`,
- [x] Проверить, что созданный плейлист сохраняется и доступен после перезапуска приложения, добавление треков в «Плейлисты» и «Избранное» работает корректно.

## Задача 2. Хранение истории поиска (DataStore)
- [x] Создать класс `SearchHistoryPreferences`, работающий с `DataStore`,
- [x] Добавить метод `addEntry` (максимум 10 запросов, без дубликатов, новые сверху),
- [x] Реализовать метод `getEntries` для получения истории,
- [x] Переделать `SearchHistoryRepositoryImpl` на использование `SearchHistoryPreferences`,
- [x] Удалить класс `DatabaseMock`.

## Дополнительно: связь «многие ко многим»
- [x] Создать таблицу `playlist_track_cross_ref` с полями `trackId` и `playlistId`,
- [x] Настроить сохранение и получение треков плейлиста через таблицу связей.