## Задача 1. Реализовать поиск треков.
- [x] Добавить выполнение настоящих поисковых запросов с использованием сетевых запросов и корутин.

## Задача 2. Реализовать экран плейлиста.
- [x] Сверстать `PlaylistScreen` по макету из Figma (используя `Column`, `Image`, `LazyColumn`),
- [x] Создать `PlaylistViewModel` с состоянием `playlist` (используя `collectAsState`),
- [x] Добавить метод `getPlaylist` в `PlaylistsRepository`,
- [x] В `PlaylistHost` реализовать коллбэк `MapsToPlaylist` при вызове `PlaylistsScreen`,
- [x] Добавить маршрут `PlaylistScreen` в навигацию с передачей аргумента `index`,
- [x] Настроить навигацию: переход на экран деталей трека, возврат на предыдущий экран по кнопке «Назад».