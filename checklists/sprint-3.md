Задача 1. Главный экран приложения

1. Создан класс `MainActivity`, объявлен в `AndroidManifect.xml`
2.  Свёрстан главный экран по макету Figma
3.  При нажатии на кнопки выводится `Toast` с названием нажатой кнопки

Задача 2. Переходы между экранами

1. Созданы `SettingsActivity` и `SearchActivity`, объявлены в `AndroidManifest.xml`
2. Вместо кода `Toast` в соответствующих обработчиках реализован переход на экраны
