## Задача 1. Добавление в «Избранное»
- [x] Создать compose-функцию `FavoritesScreen`,
- [x] Собрать состояние списка треков через `viewModel.favoriteList.collectAsState(emptyList())`,
- [x] Сверстать список избранных треков с помощью `LazyColumn` и готового компонента `TrackListItem`,
- [x] Настроить отображение заглушек для треков без обложки или во время загрузки,
- [x] Реализовать удаление трека из избранного по долгому нажатию `(onLongClick)`,
- [x] Настроить навигацию: переход на экран деталей трека по обычному клику, возврат назад по клику на стрелочку в верхней панели.

## Задача 2. Добавление обложки плейлиста
- [x] Прописать `READ_EXTERNAL_STORAGE` (до API 32) в манифесте и настроить runtime-запрос для старых версий Android.,
- [x] Добавить поле `coverImageUri: String?` в доменную модель `Playlist` и таблицу `PlaylistEntity`,
- [x] Настроить интент галереи `(GetContent)` в UI и сохранение URI во `ViewModel`,
- [x] Переделать `SearchHistoryRepositoryImpl` на использование `SearchHistoryPreferences`,
- [x] Использовать `Coil` (`AsyncImage`) для вывода выбранной картинки или заглушки на экранах создания, внутри плейлиста и в общем списке (добавив ресурсы в `strings.xml`).

## Финальное задание (Дополнительные фичи)
## Вариант 1. Удаление плейлиста
- [x] Добавить логику полного удаления плейлиста в `PlaylistsViewModel` / `PlaylistViewModel` и `DAO`,
- [x] На экране конкретного плейлиста добавить удаление через меню,
- [x] В списке плейлистов (`PlaylistsScreen`) добавить удаление по долгому нажатию (`onLongClick`),
- [x] Добавить диалог подтверждения (`AlertDialog`) перед удалением плейлиста,
- [x] Настроить навигацию: после успешного удаления возвращать пользователя на общий список плейлистов.

## Вариант 2. Удаление трека из плейлиста
- [x] Добавить логику разрыва связи трека и плейлиста в `DAO` и `ViewModel`,
- [x] Настроить вызов удаления по долгому нажатию на трек (`onLongClick`) внутри экрана конкретного плейлиста,
- [x] Добавить диалог подтверждения (`AlertDialog`) перед удалением трека из плейлиста.


